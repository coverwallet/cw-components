import React from 'react/addons';
import { ok, equal } from 'assert';
import CwComponents from '../../index';

const TestUtils = React.addons.TestUtils;

/* eslint func-names:0 */
describe('cw-components', () => {
  it('should have tests', () => {
    ok(true);
  });
});
